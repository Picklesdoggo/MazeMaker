<Window x:Class="MazeMaker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shoot House Generator" Height="400" Width="400" WindowStartupLocation="CenterScreen" Background="LightGray">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<StackPanel Margin="10" >
			<Label FontWeight="Bold">Horizontal Walls</Label>
			<RadioButton Name="btnHorizontalWide" IsChecked="True">Wide</RadioButton>
			<RadioButton Name="btnHorizontalNarrow" >Narrow</RadioButton>
		</StackPanel>

		<StackPanel Margin="10" Grid.Row="1">
			<Label FontWeight="Bold">Vertical Walls</Label>
			<RadioButton Name="btnVerticalWide" >Wide</RadioButton>
			<RadioButton Name="btnVerticalNarrow" IsChecked="True">Narrow</RadioButton>
		</StackPanel>

		<StackPanel Margin="10" Grid.Row="2">
			<Label FontWeight="Bold">Map Name</Label>
			<TextBox Name="txtName" Background="DarkGray" Text="Shoot House W X N"/>
		</StackPanel>

		<Button Name="btnStart" Content="Manually Edit Maze" Grid.Row="3" Click="btnStart_Click" Margin="10"/>

		<Button Name="btnRandom" Content="Randomly Generate Maze" Grid.Row="4" Click="btnRandom_Click" Margin="10"/>
	</Grid>
	
   
    

</Window>
